<div class="contact">
  <%= render 'layouts/iconbar' %>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-6">
        <!-- <div class="map" id="map" style='position: absolute; left: 0px; top: 0px; z-index: 3; width: 100%; height: 100%;'></div> -->
        <div class="map-frame">
          <div class="map" id="map" style='width: 100%; height: 600px;'></div>
        </div>
      </div>
      <div class="col-md-6">
        <div class="spacing">
          <div class="top-right">
            <p>THE <strong>JOY</strong> OF A TASTE WHICH HAS <strong>NEVER BEEN LOST</strong></p>
          </div>
          <%= render 'layouts/footer' %>
        </div>
      </div>
    </div>
  </div>
</div>

<script language="javascript" type="text/javascript">
  function initMap() {
    var myLatLng = {lat: 25.051415, lng: 121.5147723};

    // Create a map object and specify the DOM element for display.
    var map = new google.maps.Map(document.getElementById('map'), {
      center: myLatLng,
      scrollwheel: false,
      zoom: 17
    });

    // Create a marker and set its position.
    var marker = new google.maps.Marker({
      map: map,
      position: myLatLng,
      title: 'Hello World!'
    });
  }
</script>
